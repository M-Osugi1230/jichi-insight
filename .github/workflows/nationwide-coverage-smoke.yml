name: Nationwide Coverage Smoke

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - Deploy Pages
    types:
      - completed

permissions:
  contents: read

concurrency:
  group: nationwide-coverage-smoke-${{ github.event.workflow_run.id || github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: Verify nationwide coverage in production
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main

      - name: Check nationwide coverage pages
        run: bash scripts/check_nationwide_coverage_production.sh

      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nationwide-coverage-smoke-report
          path: nationwide-coverage-smoke-report.txt
          if-no-files-found: error
          retention-days: 14
