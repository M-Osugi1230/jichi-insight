name: Quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  materialize:
    name: Materialize Miyagi measure 16
    if: github.head_ref == 'agent/miyagi-actuals-measure16'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/miyagi-actuals-measure16
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python validation tools
        run: python -m pip install -e "[dev]"

      - name: Apply measure 16 canonical state
        run: python scripts/apply_miyagi_measure16_actuals.py

      - name: Validate materialized state
        run: |
          python scripts/validate_repository.py
          pytest \
            tests/test_miyagi_kpi_actuals_measure16.py \
            tests/test_miyagi_catalog_totals.py \
            tests/test_wave1_policy_review_queue.py
          ruff check \
            scripts/apply_miyagi_measure16_actuals.py \
            tests/test_miyagi_kpi_actuals_measure16.py \
            tests/test_miyagi_catalog_totals.py \
            tests/test_wave1_policy_review_queue.py

      - name: Commit canonical state
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            data/entities/policy/miyagi_kpi_catalog_measure16.json \
            data/catalog/miyagi_policy_review_manifest.json \
            data/catalog/wave1_policy_review_queue.json \
            apps/web/lib/miyagiActuals.ts \
            tests/test_miyagi_catalog_totals.py \
            tests/test_wave1_policy_review_queue.py
          if git diff --cached --quiet; then
            echo "Measure 16 canonical state is already current"
            exit 0
          fi
          git commit -m "Materialize Miyagi measure 16 actual linkage"
          git push origin HEAD:agent/miyagi-actuals-measure16

  repository:
    name: Repository and data
    needs: materialize
    if: always() && (needs.materialize.result == 'success' || needs.materialize.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python validation tools
        run: python -m pip install -e "[dev]"

      - name: Validate repository
        run: |
          set -o pipefail
          python scripts/validate_repository.py 2>&1 | tee repository-validation.log

      - name: Upload repository validation diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: repository-validation-diagnostics
          path: repository-validation.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Test
        run: |
          set -o pipefail
          pytest -vv 2>&1 | tee pytest.log

      - name: Upload pytest diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-diagnostics
          path: pytest.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Lint Python
        run: |
          set -o pipefail
          ruff check scripts tests 2>&1 | tee ruff.log

      - name: Upload Python lint diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: python-lint-diagnostics
          path: ruff.log
          if-no-files-found: ignore
          retention-days: 7

  web:
    name: Web
    needs: materialize
    if: always() && (needs.materialize.result == 'success' || needs.materialize.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Enable pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: |
          set +e
          pnpm build > build.log 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            tail -n 200 build.log > build-error.log
            cat build-error.log
            exit "$status"
          fi
          cat build.log

      - name: Upload build diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: web-build-diagnostics
          path: build-error.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Validate static export
        run: |
          set -o pipefail
          python scripts/validate_static_export.py 2>&1 | tee static-export.log

      - name: Upload static-export diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: static-export-diagnostics
          path: static-export.log
          if-no-files-found: ignore
          retention-days: 7
